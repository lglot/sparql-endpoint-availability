version: "3"

services:

  db_test:
    image: mysql:latest
    container_name: mysql_test
    environment:
      MYSQL_DATABASE: sparql_endpoint_availability
      MYSQL_ROOT_PASSWORD: develop
    ports:
      - "3306:3306"
